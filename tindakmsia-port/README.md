Port over the front end to a component; Reactive based system; using VueJS from what was learned

Isolate out each parallel pieces to be called

Play with pagination if possible ... as base for Objector DBs?

